// Placeholder route for Task 6. Full implementation (detail page + loader) lands there.
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_authenticated/quotations/$id')({
  component: () => null,
})
